if(CMAKE_HIP_COMPILER)
    set_source_files_properties(traversal_gpu.cu upsweep_gpu.cu PROPERTIES LANGUAGE HIP)
endif()

if(CMAKE_CUDA_COMPILER OR CMAKE_HIP_COMPILER)
    add_library(ryoanji_kernels OBJECT traversal_gpu.cu upsweep_gpu.cu rtfmm_mpole.cu)
    target_include_directories(ryoanji_kernels PUBLIC ${PROJECT_SOURCE_DIR}/src ${CSTONE_DIR})
    target_include_directories(ryoanji_kernels PRIVATE ${BLAS_INCLUDE_DIR})
    target_link_libraries(ryoanji_kernels PUBLIC BLAS::BLAS LAPACK::LAPACK)
endif()
